import Ember from 'ember';

export default Ember.Route.extend({
  // model: "new-recipe",

  // setupController: function(controller, model){
  //   controller.set('model', model.recipes);
  //   // controller.set('users', model.users);
  // }

  controllerName: 'new-recipe'
});
